# 服务器配置
PORT=8080

# 数据库配置
DATABASE_URL=postgres://postgres:password@localhost/food_ordering?sslmode=disable
# 测试数据库（可选，用于运行测试）
TEST_DATABASE_URL=postgres://postgres:password@localhost/food_ordering_test?sslmode=disable

# JWT认证配置
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
# Token过期时间：支持 h(小时), m(分钟), s(秒)
# 示例：24h (24小时), 30m (30分钟), 3600s (1小时)
JWT_EXPIRATION=24h

# S3对象存储配置（用于媒体文件上传）
# 注意：这些配置必须填写才能使用文件上传功能
S3_ENDPOINT=
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=
S3_REGION=
# S3路径样式：MinIO需要设为true，AWS S3设为false
S3_PATH_STYLE=false